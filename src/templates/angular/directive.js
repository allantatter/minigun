'use strict';

angular.module('tlDirectives')
    .directive('', function () {
        return {
            restrict: 'A',
            link: function(scope, element, attrs) {

            }
        };
    });
